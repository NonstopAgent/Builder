# Super Builder

This project is the codebase for the Super Builder platform — a full-stack application that combines a FastAPI backend with a React frontend. The system will eventually support autonomous agents that plan and execute build tasks via a task queue.
